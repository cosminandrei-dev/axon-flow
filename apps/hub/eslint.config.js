/**
 * ESLint configuration for NestJS Hub Orchestrator
 * @package apps/hub
 */

import nestConfig from "../../tooling/eslint/nest.js";

/** @type {import("eslint").Linter.Config[]} */
export default [...nestConfig];
