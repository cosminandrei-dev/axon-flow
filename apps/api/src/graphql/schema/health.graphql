scalar DateTime

type Query {
  health: HealthStatus!
}

type HealthStatus {
  status: String!
  timestamp: DateTime!
  version: String!
  services: [ServiceHealth!]!
}

type ServiceHealth {
  name: String!
  status: String!
  latency: Int
}
