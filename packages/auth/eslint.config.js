/**
 * ESLint configuration for auth package
 * @package @repo/auth
 */

import baseConfig from "../../tooling/eslint/base.js";

/** @type {import("eslint").Linter.Config[]} */
export default [...baseConfig];
